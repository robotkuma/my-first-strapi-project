{
  "kind": "collectionType",
  "collectionName": "games",
  "info": {
    "singularName": "game",
    "pluralName": "games",
    "displayName": "Games",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "Title": {
      "type": "string",
      "required": true
    },
    "Status": {
      "type": "enumeration",
      "enum": [
        "received",
        "pending",
        "approved",
        "denied"
      ],
      "required": true,
      "default": "received"
    }
  }
}
