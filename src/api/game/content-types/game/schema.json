{
  "kind": "collectionType",
  "collectionName": "games",
  "info": {
    "singularName": "game",
    "pluralName": "games",
    "displayName": "Games"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "Title": {
      "type": "string",
      "required": true
    },
    "Status": {
      "type": "enumeration",
      "enum": [
        "received",
        "pending",
        "approved"
      ],
      "required": true,
      "default": "received"
    }
  }
}
